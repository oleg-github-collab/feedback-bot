# 🚀 Повна Інструкція з Розгортання на Railway

## Передумови

- Обліковий запис GitHub
- Обліковий запис Railway (безкоштовний план підійде)
- Telegram обліковий запис
- OpenAI API ключ з кредитами

---

## Частина 1: Підготовка Коду

### 1.1 Ініціалізація Git Репозиторію

```bash
cd "/Users/olehkaminskyi/Desktop/Джейн Бот"
git init
git add .
git commit -m "Initial commit: FeedbackBot system"
```

### 1.2 Створення GitHub Репозиторію

1. Перейдіть на https://github.com/new
2. Створіть новий репозиторій (наприклад, `feedback-bot`)
3. **НЕ** додавайте README, .gitignore, чи ліцензію

### 1.3 Завантаження Коду

```bash
git remote add origin https://github.com/YOUR_USERNAME/feedback-bot.git
git branch -M main
git push -u origin main
```

---

## Частина 2: Налаштування Telegram Бота

### 2.1 Створення Бота

1. Відкрийте Telegram
2. Знайдіть [@BotFather](https://t.me/BotFather)
3. Надішліть: `/newbot`
4. Введіть назву бота (наприклад, "OpsLab Feedback Bot")
5. Введіть username бота (має закінчуватись на `bot`, наприклад: `opslab_feedback_bot`)
6. **ЗБЕРЕЖІТЬ токен** який надасть BotFather (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2.2 Отримання User ID

1. Знайдіть [@userinfobot](https://t.me/userinfobot)
2. Надішліть будь-яке повідомлення
3. **ЗБЕРЕЖІТЬ ваш User ID** (це число, наприклад: `123456789`)

---

## Частина 3: Отримання OpenAI API Ключа

### 3.1 Реєстрація

1. Перейдіть на https://platform.openai.com/signup
2. Зареєструйтесь або увійдіть

### 3.2 Створення API Ключа

1. Перейдіть в розділ "API Keys": https://platform.openai.com/api-keys
2. Натисніть "Create new secret key"
3. Дайте назву (наприклад, "FeedbackBot Production")
4. **ЗБЕРЕЖІТЬ ключ** (формат: `sk-...`) - він більше не покажеться!

### 3.3 Поповнення Балансу

1. Перейдіть в "Billing": https://platform.openai.com/account/billing
2. Додайте платіжний метод
3. Поповніть мінімум на $5

**Примітна вартість:**
- Whisper API: ~$0.006 за хвилину аудіо
- GPT-4o mini: ~$0.0001 за 1K tokens
- Приблизно 100 фідбеків = $0.50-1.00

---

## Частина 4: Розгортання на Railway

### 4.1 Створення Проекту

1. Перейдіть на https://railway.app
2. Натисніть "Login" та авторизуйтесь через GitHub
3. Натисніть "New Project"
4. Оберіть "Deploy from GitHub repo"
5. Знайдіть та оберіть ваш `feedback-bot` репозиторій
6. Натисніть "Deploy Now"

### 4.2 Додавання PostgreSQL

1. У вашому проекті натисніть "New"
2. Оберіть "Database"
3. Оберіть "Add PostgreSQL"
4. Railway автоматично створить базу даних та зв'яже її з вашим додатком

### 4.2.1 Додавання Redis

1. У вашому проекті натисніть "New"
2. Оберіть "Database"
3. Оберіть "Add Redis"
4. Railway автоматично створить Redis та зв'яже його з вашим додатком

### 4.3 Налаштування Змінних Середовища

1. Оберіть ваш сервіс (не database)
2. Перейдіть на вкладку "Variables"
3. Натисніть "Raw Editor"
4. Вставте наступне, **замінивши значення на свої**:

```bash
# Database (автоматично додається Railway)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Redis (автоматично додається Railway)
REDIS_URL=${{Redis.REDIS_URL}}

# Phoenix - згенеруйте SECRET_KEY_BASE локально
# Виконайте: mix phx.gen.secret
SECRET_KEY_BASE=ваш_згенерований_секрет_64_символи

# Домен вашого додатку (спочатку може бути тимчасовим)
PHX_HOST=your-project.up.railway.app

# Telegram
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
ALLOWED_USER_ID=123456789

# OpenAI
OPENAI_API_KEY=sk-ваш_ключ_тут

# Інші налаштування
MIX_ENV=prod
PORT=8080
POOL_SIZE=10
```

### 4.4 Генерація SECRET_KEY_BASE

На вашому локальному комп'ютері:

```bash
# Встановіть Elixir якщо ще не встановлено
brew install elixir

# Згенеруйте секрет
mix phx.gen.secret
```

Скопіюйте вивід та використайте як `SECRET_KEY_BASE`.

### 4.5 Виправлення PHX_HOST

1. Після першого деплою, Railway надасть вам URL
2. Скопіюйте домен (без `https://`)
3. Оновіть змінну `PHX_HOST` на правильне значення
4. Редеплой відбудеться автоматично

---

## Частина 5: Запуск Міграцій

### 5.1 Через Railway CLI

Встановіть Railway CLI:

```bash
# Mac
brew install railway

# Windows (PowerShell)
iwr https://get.railway.app/install.ps1 | iex
```

Виконайте міграції:

```bash
railway login
railway link
railway run mix ecto.migrate
railway run mix run priv/repo/seeds.exs
```

### 5.2 Альтернатива: Через Web Console

1. У Railway перейдіть до вашого сервісу
2. Перейдіть на вкладку "Deployments"
3. Знайдіть успішний deployment
4. Натисніть на нього
5. Натисніть "View Logs"
6. Натисніть іконку терміналу справа вгорі
7. Виконайте:

```bash
/app/bin/feedback_bot eval "FeedbackBot.Release.migrate"
/app/bin/feedback_bot eval "FeedbackBot.Release.seed"
```

**Примітка:** Для цього потрібно додати release модуль.

---

## Частина 6: Додавання Release Tasks (Опціонально)

Створіть файл `lib/feedback_bot/release.ex`:

```elixir
defmodule FeedbackBot.Release do
  @moduledoc """
  Release tasks для виконання міграцій та seeds у production
  """

  @app :feedback_bot

  def migrate do
    load_app()

    for repo <- repos() do
      {:ok, _, _} = Ecto.Migrator.with_repo(repo, &Ecto.Migrator.run(&1, :up, all: true))
    end
  end

  def seed do
    load_app()

    for repo <- repos() do
      {:ok, _, _} = Ecto.Migrator.with_repo(repo, fn _repo ->
        seed_script = Path.join([Application.app_dir(@app), "priv", "repo", "seeds.exs"])

        if File.exists?(seed_script) do
          Code.eval_file(seed_script)
        end
      end)
    end
  end

  def rollback(repo, version) do
    load_app()
    {:ok, _, _} = Ecto.Migrator.with_repo(repo, &Ecto.Migrator.run(&1, :down, to: version))
  end

  defp repos do
    Application.fetch_env!(@app, :ecto_repos)
  end

  defp load_app do
    Application.load(@app)
  end
end
```

Тепер міграції можна виконати:

```bash
railway run "/app/bin/feedback_bot eval 'FeedbackBot.Release.migrate()'"
railway run "/app/bin/feedback_bot eval 'FeedbackBot.Release.seed()'"
```

---

## Частина 7: Налаштування Custom Domain (Опціонально)

### 7.1 Через Railway

1. Перейдіть у ваш сервіс
2. Вкладка "Settings"
3. Розділ "Domains"
4. Додайте власний домен або використайте railway.app піддомен

### 7.2 Оновлення PHX_HOST

Не забудьте оновити змінну `PHX_HOST` на новий домен.

---

## Частина 8: Перевірка та Тестування

### 8.1 Перевірка Веб-додатку

1. Відкрийте https://your-project.up.railway.app
2. Повинен відкритись Dashboard
3. Перевірте що список співробітників завантажився

### 8.2 Перевірка Telegram Бота

1. Знайдіть вашого бота в Telegram (за username)
2. Надішліть `/start`
3. Повинен з'явитись список співробітників з inline кнопками
4. Оберіть співробітника
5. Надішліть голосове повідомлення (10-30 секунд)
6. Дочекайтесь обробки (може зайняти 10-30 секунд)
7. Перевірте результат

### 8.3 Перевірка Логів

У Railway:
1. Перейдіть до "Deployments"
2. Оберіть активний deployment
3. Перегляньте "Logs"

Шукайте повідомлення про:
- Успішний запуск Phoenix
- Підключення до БД
- Запуск Telegram бота

---

## Частина 9: Моніторинг та Обслуговування

### 9.1 Перегляд Метрик

Railway надає базові метрики:
- CPU usage
- Memory usage
- Network traffic

### 9.2 Перезапуск Сервісу

Якщо щось пішло не так:
1. Перейдіть до сервісу
2. Три крапки → "Restart"

### 9.3 Перегляд Бази Даних

```bash
railway connect postgres
```

Або через Railway UI:
1. Оберіть PostgreSQL сервіс
2. Вкладка "Data"

---

## Частина 10: Оновлення Додатку

### 10.1 Внесення Змін

```bash
# Внесіть зміни у код
git add .
git commit -m "Опис змін"
git push origin main
```

Railway автоматично виявить зміни та почне новий деплой.

### 10.2 Міграції при Оновленні

Якщо ви додали нові міграції:

```bash
railway run mix ecto.migrate
```

---

## Troubleshooting

### Проблема: Бот не відповідає

**Рішення:**
1. Перевірте логи Railway
2. Переконайтесь що `TELEGRAM_BOT_TOKEN` правильний
3. Перевірте що `ALLOWED_USER_ID` відповідає вашому ID
4. Перезапустіть сервіс

### Проблема: OpenAI API помилки

**Рішення:**
1. Перевірте баланс: https://platform.openai.com/account/billing
2. Перевірте що `OPENAI_API_KEY` правильний
3. Перевірте rate limits

### Проблема: Database connection errors

**Рішення:**
1. Перевірте що `DATABASE_URL` встановлено
2. Переконайтесь що PostgreSQL сервіс запущений
3. Виконайте міграції знову

### Проблема: 500 Server Error

**Рішення:**
1. Перевірте логи Railway
2. Переконайтесь що `SECRET_KEY_BASE` встановлено
3. Перевірте що всі міграції виконані

---

## Корисні Команди

```bash
# Логи в реальному часі
railway logs

# Підключення до БД
railway connect postgres

# Виконання Elixir команд
railway run iex -S mix

# Відкрити додаток
railway open

# Перегляд змінних
railway variables
```

---

## Контрольний Список

- [ ] Код завантажено на GitHub
- [ ] Створено Telegram бота через @BotFather
- [ ] Отримано Telegram User ID
- [ ] Створено OpenAI API ключ
- [ ] Поповнено баланс OpenAI
- [ ] Створено проект на Railway
- [ ] Додано PostgreSQL
- [ ] Налаштовано всі змінні середовища
- [ ] Успішно задеплоєно
- [ ] Виконано міграції
- [ ] Виконано seeds
- [ ] Перевірено веб-додаток
- [ ] Перевірено Telegram бота
- [ ] Протестовано обробку аудіо

---

## Підтримка

Якщо виникли проблеми:
1. Перевірте логи Railway
2. Перевірте всі змінні середовища
3. Переконайтесь що міграції виконані
4. Перезапустіть сервіс

---

**Вітаємо! 🎉 Ваша система аналізу фідбеку готова до використання!**
